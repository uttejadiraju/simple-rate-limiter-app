# simple-rate-limiter-app
A express app for simple demonstration of rate limit.

Prerequisites:-

1) Install node in your system.
2) Run npm install in the project directory.

Launch:-
1) Run "node index.js".
2) Hit http://localhost:3000 in browser.
